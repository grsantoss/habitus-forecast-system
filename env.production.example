# Arquivo de exemplo para produção
# Copie para .env e ajuste os valores

# ============================================
# PostgreSQL Database
# ============================================
POSTGRES_DB=habitus_forecast
POSTGRES_USER=habitus
POSTGRES_PASSWORD=ALTERE_ESTA_SENHA_PARA_UMA_SEGURA
POSTGRES_PORT=5432

# ============================================
# Backend Flask
# ============================================
# Gere uma chave secreta forte:
# python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=ALTERE_ESTA_CHAVE_PARA_UMA_ALEATORIA_SEGURA

# URL do banco de dados (usar nome do serviço Docker 'db')
DATABASE_URL=postgresql://habitus:ALTERE_ESTA_SENHA_PARA_UMA_SEGURA@db:5432/habitus_forecast

# Ambiente
FLASK_ENV=production
FLASK_DEBUG=False

# Servidor
PORT=5000
WORKERS=4

# CORS - Domínios de produção permitidos
CORS_ORIGINS=https://app.habitusforecast.com.br

# Upload
MAX_CONTENT_LENGTH=16777216

# ============================================
# Logging (Opcional)
# ============================================
LOG_LEVEL=INFO
LOG_FORMAT=json

# ============================================
# Rate Limiting (Opcional)
# ============================================
# RATELIMIT_DEFAULT=200 per hour
# RATELIMIT_STORAGE_URI=memory://
# Para Redis: redis://localhost:6379/0

# ============================================
# Monitoramento (Recomendado para Produção)
# ============================================
# Sentry - Monitoramento de erros
# 1. Crie uma conta em https://sentry.io
# 2. Crie um novo projeto (Flask/Python)
# 3. Copie o DSN e cole abaixo
# SENTRY_DSN=https://seu-dsn-do-sentry@sentry.io/projeto

# Versão da aplicação (opcional, usado pelo Sentry)
# APP_VERSION=1.0.0

# ============================================
# Variáveis para GitHub Actions Deploy
# ============================================
# Configure no GitHub Secrets:
# - SERVER_HOST=seu-servidor.com
# - SERVER_USER=usuario-ssh
# - SSH_PRIVATE_KEY=chave-privada-ssh
# - SSH_PORT=22 (opcional)

# Caminho da aplicação no servidor (ajustar no deploy.yml se diferente)
SERVER_APP_DIR=/var/www/habitus-forecast-system

# ============================================
# Frontend (Vite)
# ============================================
# URL da API backend para o frontend
# IMPORTANTE: Configure esta variável antes de fazer o build do frontend em produção
VITE_API_URL=https://app.habitusforecast.com.br/api

